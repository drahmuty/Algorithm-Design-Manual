#include <stdio.h>

struct list {
    item_type item;
    struct list *next;
} list;

int main(void)
{
    return 0;
}

void reverse(*list) {
    *p;
    last = NULL;
    
    if (list->next == NULL) {
        # set item as the root
        return;
    }
    else {
        last = list;
        
    }
}

/*
Writing psuedo code instead.

Given a linked list...
*p;
last = NULL;
while (l->next != NULL) {
    p = l;
    l->next = last;
    }
    



*/
